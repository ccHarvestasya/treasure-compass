# リポジトリ作業指針

## 役割

この AGENTS.md は、作業手順、情報探索、変更範囲、検証および報告方法を定義する。アプリの機能仕様そのものではない。仕様や利用者向けの約束は、ユーザーの依頼、README、正式文書、実装、テスト、静的データを照合して扱う。

## プロジェクト概要

このリポジトリは、FFXIV のトレジャーハントを支援するブラウザアプリケーション Treasure Compass である。パーティメンバーのマップ情報を受け取り、地図上のポイントと巡回順を表示し、進捗とチャット用マクロを扱う。

主な実体は React 19 / TypeScript / Vite、Zustand、Tailwind CSS、public/json/ のマップデータ、public/img/ の地図画像、tests/unit/ の Vitest テストである。状態の一部はブラウザの localStorage に保存される。

依頼にない機能、画面、外部サービス、サーバー、認証、永続化方式を推測で追加しない。

## Repository Map

- README.md: 機能、セットアップ、対応範囲、ディレクトリ構成
- package.json / pnpm-lock.yaml: 依存関係と検証コマンド
- src/components/: 画面と UI
- src/constants/: グレード、座標、表示、保存キー
- src/types/: マップ、ポイント、メンバー、経路の型
- src/store/: Zustand 状態と localStorage 境界
- src/utils/ / src/hooks/: 入力解析、計算、マップ読み込み
- public/json/: グレードごとのマップポイントデータ
- public/img/: 地図画像と編集用画像
- tests/unit/: ユニットテスト
- .agents/skills/: 作業補助。アプリ仕様の正本ではない

docs/ が将来追加された場合は、存在する文書とユーザーの依頼に従って必要な範囲だけ参照する。存在しない文書や成果物を前提にしない。

## 作業開始

1. ユーザーの依頼、変更可能な範囲、求められた検証を確認する。
2. 対象作業に対応する .agents/skills/ の Skill を読む。
3. git status --short と対象の現在差分を確認する。
4. README.md、package.json、対象の source、test、JSON、画像を必要な範囲で確認する。
5. 依存ライブラリの公式資料や画像ライセンスは、必要な場合だけ確認する。

既存のユーザー変更を取り消さない。無関係なコード、画像、依存関係、文書を調査・変更しない。

## Source of Truth

根拠は次の順で扱う。

1. ユーザーの明示的な依頼
2. 対象機能の承認済み要件・設計・仕様（存在する場合）
3. README と既存の利用者向け契約
4. 型、定数、実装、静的データ、テスト
5. package.json、Vite / TypeScript 設定、依存ライブラリの公式資料

実装やテストの現在値だけで、新しい仕様や互換性を正当化しない。依存ライブラリの API と、このアプリの利用者向け挙動を同一視しない。

特に次の外部可視動作は、変更前後で根拠を確認する。

- グレード、メンバー登録、チャット一括入力、地図、経路順、完了状態
- マクロの文字列、座標単位、表示上の丸め
- localStorage の既存キー、保存対象、復元・削除
- GRADE_CONFIG と JSON・画像の対応
- ローディング、空状態、エラー、解析不能入力の扱い

## 競合と未決定事項

資料、実装、テスト、データが競合する場合、勝手に統合・平均化・補完しない。対象機能、資料の役割と更新時点、利用者向け契約か内部実装か、互換性の根拠、影響範囲を確認する。

解消できない場合は、確認できた事実、未決定の判断、影響、選択肢、作業継続の可否を分けて報告する。実装の都合だけで未決定事項を確定しない。

## Scope Discipline

依頼や仕様にない機能、画面、設定、保存項目、入力形式、エラー、フォールバック、外部連携、依存、将来拡張用の公開抽象化を追加しない。既存保存データを無断で移行・破棄しない。内部実装の改善は、利用者から観測できる動作を変えない範囲に限る。

## アプリ固有の注意

- GRADE_CONFIG、対応する JSON、画像プレフィックス、ファイル名の対応を崩さない。新グレードは依頼がある場合だけ追加する。
- マップ JSON、localStorage、チャット貼り付けは外部入力に近いデータとして扱い、型・必須項目・範囲・参照先を確認する。
- 解析不能行、空白、全角記号、座標境界、未知マップの既存挙動を確認し、推測変換を追加しない。
- 経路計算の座標単位、距離、テレポートコスト、順序、空入力を変更する場合は、仕様とテストを同時に確認する。
- public/img/ の実行時画像と編集用画像は、参照関係、ライセンス、差分を確認する。未確認の資産を追加しない。
- 依頼なしにサーバー API、ファイルアクセス、外部送信、計測、広告、認証を導入しない。

## セキュリティとデータ

このアプリは名前、チャット入力、座標、ブラウザ保存データを扱う。次を守る。

- 外部入力、静的 JSON、localStorage を検証前に信用しない。
- ユーザー入力を HTML として解釈しない。dangerouslySetInnerHTML や eval を追加しない。
- 入力、保存データ、環境変数を不要にログ、例外、画面、レビュー成果物へ出さない。
- 異常データでクラッシュ、無限ループ、過大な計算、誤った成功を起こさない。
- localStorage の破損・想定外形状は安全に扱い、未確認データを正常状態として採用しない。
- 新しい外部通信や個人情報収集を依頼なしに追加しない。

## 変更時の確認

対象が UI、状態、純粋関数、静的データ、画像、設定、依存関係のどれかを先に区別する。次の変更は型、テスト、README、データとの整合を確認する。

- コンポーネントの props、イベント、表示条件
- Zustand の状態、アクション、永続化対象
- src/types/ のデータ形状
- src/constants/ の保存キー、座標、経路、グレード設定
- public/json/ のフィールド、ポイント分類、ファイル名
- マクロ、ルート順、完了、ローディング、エラー表示
- package.json の script、依存関係、build 設定

不要な依存更新、lockfile の無関係な変更、自動生成物の更新を行わない。

## Validation

検証前に変更ファイルを確定する。作業中は次を確認する。

- git diff --name-only
- git diff --cached --name-only
- git ls-files --others --exclude-standard

変更分類は次のとおり。

- AGENTS.md と .agents/** だけ: Skill の構造・参照・Markdown・差分・状態を確認し、アプリの lint / test / build は実行しない。
- README.md や docs/** だけ: Markdown、リンク、文書整合性、差分・状態を確認し、コード検証は依頼がない限り実行しない。
- src/**、tests/**、package.json、pnpm-lock.yaml、TypeScript / Vite / ESLint 設定: 影響に応じて lint、unit test、build を実行する。
- public/json/**: JSON 構文・形状・参照先と、関連する読み込み・解析・計算を確認する。
- public/img/**: 実行時参照、形式、ライセンス、差分を確認する。画像だけの変更へコードテストを自動拡大しない。

利用可能な標準コマンドは pnpm lint、pnpm test、pnpm run build、必要時の pnpm run dev / pnpm run preview である。存在しない script や formatter を前提にしない。未実行は成功扱いにせず、対象外は NOT APPLICABLE / SKIPPED、実行不能は Not validated として報告する。

該当する場合は、空・部分・不正入力、全角半角、座標境界、未知マップ、空メンバー、重複、最大人数、複数マップ、JSON ロード失敗、想定外形状、localStorage 破損、グレード変更、リセット、再計算、マクロの空配列と丸めを確認する。

## 文書フェーズ

文書化する場合も、抽象度を混同しない。

- Concept: 背景、課題、対象ユーザー、提供価値、スコープ、対象外、責任境界、成功状態
- Requirements: 外部から見て満たす必要がある能力、制約、品質、責任、受け入れ条件
- Specification: 入力、出力、状態遷移、外部データ形式、エラーなどの正確な外部契約
- Design: Specification を実現する内部責務、構造、依存、状態・データ所有、失敗境界
- Implementation: 承認済み Design / Specification をコード、設定、テスト、静的データへ反映
- README: 現在の機能、セットアップ、制約、検証方法

上流文書を下流の実装から逆生成しない。レビューは対象を直接修正せず、根拠のある指摘と引継ぎを記録する。

## Git と報告

ユーザーの明示なしに commit、push、tag、publish、remote 変更を行わない。完了時は変更内容、根拠、検証結果、未検証範囲、残存する仮定・未決定事項を区別して報告する。入力、保存データ、環境変数、個人情報を報告へ含めない。

このリポジトリでは、ユーザーが commit と push を明示的に依頼した場合、`origin`（`git@github.com:ccHarvestasya/treasure-compass.git`）を承認済みの push 先として扱う。宛先の確認や、同じ push 許可の再確認を繰り返さない。ただし、実行環境が要求する承認・安全確認はこの記載では変更できないため、必要な場合はその結果を報告する。

コミットを作成する場合は、タイトルと本文を日本語にし、タイトルの後に空行と - 箇条書きの本文を置く。push 前に git show -s --format='%B' HEAD で確認する。
