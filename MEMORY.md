# Project Memory

`MEMORY.md` は継続コンテキスト用の索引であり、正式な契約・設計の正本ではない。機能の判断は、ユーザー依頼と [Concept](docs/concept/concept.md) → [Requirements](docs/requirements/requirements.md) → [Specification](docs/specification/specification.md) → [Design](docs/design/design.md) を優先する。

## Project Overview

- FFXIV の巡回支援ブラウザアプリ。Treasure Compass はパーティの宝箱を、Mob Compass はモブを対象に、マップ間遷移を抑えた巡回と進行管理を支援する。
- v1 では Treasure Compass と Mob Compass を利用者から見て別アプリとして提供する。Treasure はメンバーごとの現在対象が 1 件、Mob はモブごとの候補地点が 0..多という意味の違いを維持する。
- プロダクトは利用者が入力・選択・完了した情報を扱う支援ツールであり、ゲーム内状態の自動取得・判定やゲーム内チャットへの自動投稿は行わない。

## Current Decisions

- Treasure Compass は現在運用中であり、外部仕様を大きく変更する必要はない。Mob Compass は新規作成であり、UI は未確定である。
- Treasure Compass と Mob Compass は同一アプリ内の製品切替ではなく、別アプリに分ける。同じリポジトリ内で管理し、Treasure と Mob を別エントリ・別 URL としてビルド・公開する。リポジトリ自体は分割しない。地図表示、地点選択、経路描画などの基本機能は共有してよいが、画面、入力フロー、周回状態、保存、消去はアプリごとに分離する。
- Treasure Compass と Mob Compass はどちらもスマートフォン表示へ対応し、小さい画面でも対象登録、地図上の地点選択、巡回経路の確認、完了・取消、モード切替、クリアを利用できるようにする。スマートフォンは縦向きを主対象とし、横向きでも主要操作が不能にならないようにする。
- Treasure Compass の外部仕様は大きく変えず、Treasure と Mob の操作感は可能な範囲で統一する。地図上のマスター地点をクリックした時点で登録すること、巡回リストを選択・ドラッグして順序を扱うこと、完了と取消、確認付きクリア、現在地点と完了状態の視覚的な区別は共通の操作規則とする。チャット一括入力、固定メンバースロット、Mob のランク別探索など、用途固有の入口や進行は無理に共通化しない。
- Treasure も Mob と同様、手動で巡回順を並べ替えた後は、メンバーの追加・地点変更・削除で既存の相対順序を変えない。追加対象は末尾へ加え、地点変更は同じ位置を維持し、削除は該当対象だけを外す。利用者が「経路自動計算」を明示的に実行した場合だけ、自動順序へ戻して並べ直す。
- Treasure も Mob と同様、手動／自動の状態、巡回順序、完了状態、現在の巡回地点を周回状態として保存し、ブラウザの再読み込み後にそのまま復元する。
- Mob Compass は使い方が異なる個人利用と団体利用を、画面上では「ソロ」「パーティ」という名称の別モードに分け、両モードの登録対象を同じ周回へ混在させない。「ソロ」はデイリー・ウィークリー手配書の一般モブと B モブまでを対象とし、「パーティ」は共有された A モブ以上だけを対象とする。登録内容と進捗はモードごとに独立して保持し、モードを切り替えても削除せず、戻ったときに続きから再開できるようにする。
- Mob Compass の画面上部には「ソロ」「パーティ」の2つのモード切替ボタンを常設し、現在選択中のモードを識別できるようにする。Mob Compass を開いたときは前回使用していたモードを復元し、そのモードの保存済み状態を表示する。
- Mob Compass の左パネルは Treasure の操作構造に合わせて「登録」「巡回経路」の2タブに分け、その上部に「ソロ」「パーティ」のモード切替を置く。モブを登録しても自動で「巡回経路」へ切り替えず、「登録」に留まって連続登録できるようにする。巡回経路への切替は利用者が明示的に行う。
- Mob Compass の「登録内容をすべて消去」は現在開いているモードだけを対象とし、もう一方のモードの登録内容と進捗は保持する。実行前に対象モードを明示した確認ダイアログを表示し、利用者が確認した場合だけ消去する。
- Treasure Compass の「クリア」は Treasure の登録内容と進捗だけを対象とする。Mob Compass の「クリア」は現在開いている「ソロ」または「パーティ」だけを対象とし、別アプリや別モードの状態を消去しない。
- Mob Compass の登録入口は利用場面で異なる。B モブまでの個人利用では、利用者が手配書の対象を名前で検索して登録し、現地で探索する。団体利用では、他者からゲーム内チャットで受け取った報告を見ながら、報告ごとにモブ名を検索し、マップ上の候補地点をクリックして手動登録する。不定形チャットの一括解析や専用マクロによる一括登録は扱わない。
- Mob Compass の個人利用でモブ名を選択した場合、そのモブについてマスターデータに登録済みの候補地点をすべて、同じモブの候補集合へ一括登録する。
- Mob Compass は登録時に必要な情報と許可する操作をランクで分ける。B モブ以下はモブ名の選択だけでマスターの全候補地点を登録する。A モブ以上は、モブ名を選択した後、そのモブの出現マップ上に表示されたマスター登録済み候補地点をクリックすると、その一地点を現在地点として即時登録する。登録済みモブはモブリストから再選択して候補地点を選び直せる。いずれのランクでも X/Y 座標の自由入力やマップ上の任意地点登録は行わない。
- B モブ以上は同時に複数体出現しない。A モブ以上について同じモブの別候補地点が再報告・再登録された場合は、複数候補として追加せず、以前の登録地点を新しく選択した地点で置き換える。
- B モブはマスター上の候補地点のいずれかに存在し、利用者が現地で探す必要があるため、全候補地点を対象にした最短の探索ルートを提示する。
- B モブのマップ内探索ルートは、利用するエーテライトを開始地点とする。同じマップに利用可能なエーテライトが複数ある場合は、全対象地点を巡る総距離が最短になるエーテライトをアプリが自動選択する。デイリーの一般モブ討伐とウィークリーの B モブ探索を同じ周回へ混在させられるようにする。
- 同じマップにデイリー一般モブとウィークリー B モブが登録されている場合は、一般モブの討伐地点と B モブの全候補地点を統合し、エーテライトから始まる一つの最短マップ内探索ルートとして算出する。
- パーティモードで同じマップに A モブ以上の報告地点が複数登録されている場合も、全報告地点を統合し、総距離が最短になるエーテライトを起点とした最短のマップ内巡回順を算出する。
- マップ内探索ルートの距離はゲーム内 X/Y 座標の二次元直線距離で評価する。地点が X/Y 上で重なるケースはほぼないため Z は保持せず、地点識別・表示・経路評価に使用しない。
- 探索途中で B モブを発見・討伐済みにした場合は、その B モブの未訪問候補地点を残りの探索ルートから除外し、発見地点を新しい開始地点として、残っている一般モブと B モブ候補のマップ内探索ルートを再計算する。
- 自動ルートの進行中に一般モブまたは A モブ以上の地点を完了した場合も、同じマップに未完了地点が残っていればエーテライトへ戻らず、完了地点を現在地点として残りのマップ内ルートを再計算する。
- 自動ルートの巡回途中で同じマップのモブを追加した場合、または A モブ以上の登録地点を変更した場合も、最後に完了または探索した地点を現在地点として残りのマップ内ルートを再計算する。
- 最後に完了または探索した現在地点は進捗の一部として保存し、ブラウザを閉じて後から再開した場合も復元して、そこから残りの巡回を継続する。
- 周回は一日限りの利用を前提とするため、保存済みの現在地点をエーテライトへ戻す専用の「エーテライトから再開」操作は設けない。
- デイリー一般モブの必要討伐数や途中の討伐数はアプリで管理しない。利用者が必要数を倒した後、モブ単位の完了ボタンを押して完了させる。
- 完了ボタンの誤操作に備え、Treasure と Mob のどちらも完了済みの各対象に「取消」を表示し、対象ごとに未完了へ戻せるようにする。完了した対象は進捗一覧の元の位置に完了表示で残し、現在の巡回対象からだけ除外する。取消時は、手動順序では元の位置で巡回対象へ戻し、自動 route では保存中の現在地点から再計算する。Mob の B モブについては、そのモブを完了する直前の候補地点ごとの探索状態も復元する。一般的な操作履歴や任意時点への巻き戻しは提供せず、取消対象以外の後続操作を巻き戻さない。
- デイリー一般モブに複数の出現候補地点がある場合は、B モブのように全候補地点を最初から巡らず、同じマップの統合探索ルート全体が最短になる一地点だけを現在の訪問先として採用する。
- 「次の地点へ」は B モブだけに提供する。B モブが現在の候補地点にいなければ、その地点を探索済みとして残りの探索ルートから外し、現在地点から次の候補へ進む。誤操作した場合は、直前の「次の地点へ」1回だけを取り消し、探索済みにした地点、順序、現在地点を操作直前の状態へ戻せるようにする。過去の探索済み地点一覧から任意の地点を戻す機能は設けない。一般モブはすぐ再出現するため「次の地点へ」を提供せず、採用地点で必要数を倒して完了する。
- B モブの全候補地点を探索しても見つからない場合は完了にせず、「未発見」と表示する。利用者が「探索をやり直す」を明示すると、全候補地点を未探索へ戻し、その時点の現在地点から探索ルートを再計算する。時間経過による自動リセットは行わない。
- Mob Compass の対象登録には、拡張エリアや種別・ランクによる絞り込みに加え、モブ名を文字入力して候補を絞り込める検索機能を設ける。検索候補は現在モードの対象範囲に限定し、「ソロ」では一般モブから B モブまでだけを表示し、「パーティ」では A モブ以上だけを表示する。名前検索は正式名と登録済み別名を対象とする部分一致で候補を表示し、利用者が既知モブを選択した時点で登録対象を確定する。部分一致した文字列だけから対象を推測登録しない。
- ルート評価は異なるマップへの遷移回数を第一に最小化する。同数時はテレポ料金とロード時間の Pareto 関係を補助評価し、なお同順位なら同順位として扱う。詳細は Specification §6。
- Mob の candidate membership、master classification（`confirmed` / `candidate`）、route-selected、user-confirmed、completion は独立した意味であり、相互に暗黙昇格させない。
- 自動 route と手動順序は別状態。自動 route の利用中は、モブの登録、地点変更、完了によって対象状態が変わるたびに残りの巡回ルートを自動再計算し、必要に応じて順番を入れ替える。手動で並べ替えた後は既存の手動順序を自動で崩さず、新しく登録したモブを末尾へ追加する。手動順序中にモブを完了した場合は、そのモブだけを巡回対象から外して残りの相対順序を維持し、A モブ以上の地点を選び直した場合も並び位置を変えず地点だけを置き換える。手動から自動への移行と順番の再編成は、利用者が「経路自動計算」を明示的に実行し、計算が成功した場合だけ成立する。失敗時に古い route を現在結果として再利用しない。
- ソロの手動順序は訪問地点単位で編集する。B モブを一つのまとまりとして並べ替えるのではなく、そのモブの各候補地点を一般モブの訪問地点と同じ単位で個別に並べ替えられるようにする。手動順序中に B モブを新規登録した場合は、既存順序を変えず、その B モブの全候補地点だけを既存順序の末尾地点から近い順に並べて末尾へ追加する。手動順序が空なら、そのマップのエーテライトから近い順に並べる。
- Mob の案内は `MOB-COMPASS/1` の完全 snapshot として検証・入出力する。`run`、`revision`、`issued-at` を含み、同一 run の新旧判定は revision と意味上の snapshot で行う（端末時計で順序を決めない）。
- 保存書込みに失敗した状態変更または案内出力は失敗として扱い、current state・最後に正常保存された state・Mob 世代情報を変更しない。部分状態や自動 retry/pending state を残さない。
- Treasure と Mob は別 session root・別保存境界とし、相互変換や自動互換を行わない。Design では、Route Planner を read-only、Application / Session Coordinator を route 採用と logical commit の所有者としている。

## Constraints

- 団体利用で受け取るゲーム内チャットは、各参加者が作成したマクロにより書式が統一されない。チャット解析は v1 の対象外とし、利用者から要望が出た場合に、利用者とのチャット形式の合意を前提として改めて検討する。
- 参加者向け閲覧画面、共有同期・共同編集、権限移譲、サーバー・認証、ゲーム連携、長期履歴・統計・通知は v1 の対象外。
- 手動入力、貼り付け案内、localStorage、静的 master data はすべて未信頼入力として、構文・型・範囲・参照・状態整合性を検証してから採用する。破損した保存は部分復元しない。master 参照だけが解決不能な場合は周回情報を保持し、該当地点を未確認・計算対象外として扱う。
- マスターデータと利用者の周回情報を混在させない。マスターデータの正確性・更新・出典・画像利用条件は管理者の責任であり、公開前の未確認情報を有効扱いしない。
- 既存のグレード設定、対応 JSON、画像の参照関係、localStorage の利用者可視契約を、根拠なく変更・移行・破棄しない。

## Conventions

- 根拠の優先順位と作業・検証・報告の規約は [AGENTS.md](AGENTS.md) を正とする。既存変更を保全し、依頼外の機能、外部サービス、依存更新、保存形式変更を追加しない。
- 変更前に対象の現状・差分を確認し、変更種別に応じた検証を行う。docs-only の変更にはアプリの lint / test / build を自動で広げない。
- 正式文書とレビュー成果物は工程ごとに分ける。実装・テストから上流の要求や仕様を逆生成せず、矛盾は勝手に補完しない。
- commit / push / publish は明示依頼時だけ行う。commit メッセージは日本語タイトル、空行、`- ` 箇条書き本文とする。

## Current State

- 正式文書は Concept、Requirements、Specification、Design まで存在する。最終レビューはそれぞれ [Concept Review 003](docs/reviews/concept/concept-review-003.md)、[Requirements Review 005](docs/reviews/requirements/requirements-review-005.md)、[Specification Review 005](docs/reviews/specification/specification-review-005.md)、[Design Review 002](docs/reviews/design/design-review-002.md) で `READY`。Design の DR-001〜DR-004 と Specification の persistence write failure gap は解消済み。
- 現在のブランチは `maintenance/add-mob-compass`。Treasure と Mob の実装・unit test はリポジトリに存在するが、`docs/reviews/implementation/` の実装レビュー成果物はまだない。正式契約への実装適合は未レビューである。
- 現在のリポジトリ実体は React / TypeScript / Vite、Zustand、Tailwind を用いるブラウザアプリで、静的地図・地点データと browser localStorage を扱う。

## Open Issues

- スマートフォン表示は縦向きを主対象とし、横向きでも主要操作を利用可能にする。地図と登録／巡回パネルの配置、対応する画面幅と操作検証条件は正式な Requirements / Specification で定める必要がある。
- パーティモードで A モブ以上を選択した後の登録 UI は、Treasure の地点選択と同様のマップダイアログを開き、候補マーカーのクリックで即登録して閉じる案を暫定採用している。具体的な UI を正式化する際に再確認する。
- 現在の Treasure 実装では、上部の `clearAllData` が Treasure と Mob の両状態を同時に初期化する。公開単位および Mob のソロ／パーティごとに状態を分離し、現在の対象だけを消去する決定と競合するため、正式な消去範囲と実装を揃える必要がある。
- 現在の実装は一つの画面内で Treasure / Mob を切り替え、両者を一つの保存スナップショットで扱っている。同一リポジトリ内の別エントリ・別 URL として公開しつつ地図の基本機能を共有する決定に合わせ、ビルド入口、公開先、保存境界、共通機能の責任範囲を正式文書と設計で再定義する必要がある。
- 現在の Treasure 実装では、手動で巡回順を並べ替えた後も、メンバーの登録・地点変更・削除によって自動計算へ戻り、手動順序が失われる。明示的な「経路自動計算」まで既存順序を維持する決定に合わせ、正式文書と実装を更新する必要がある。
- 現在の Treasure 保存形式はグレードとメンバーだけを保持し、巡回順序、手動順序かどうか、完了、現在ステップを保存対象に含めない。これらを保存・復元する決定に合わせ、現在の README・正式文書・保存形式・実装を更新し、既存保存データの互換性を定める必要がある。
- 現在のモブ用マスターモデルにはランク項目がない。モブ選択時に B モブ以下は全候補地点を一括登録し、A モブ以上はマップ上で選択した一候補地点だけを登録するため、モブのランクをマスター情報として追加する必要がある。具体的なデータ表現と正式文書への反映は未着手。
- 個人利用の全候補地点一括登録は、現在の実装が要求する「モブと候補地点を一つずつ選択して登録」と異なる。正式文書へ反映する工程と実装変更は未着手。
- 現在の Specification は、同じモブの別地点入力を候補集合への追加としている。A モブ以上ではマスター候補地点から一地点を選び、再登録時は以前の地点を置き換える決定に合わせ、ランク別の登録・重複・訂正規則を正式文書で更新する必要がある。
- 現在の Specification は完了解除を v1 の必須操作としていない。誤操作に備えて完了および B モブの「次の地点へ」の取消を提供する決定に合わせ、取消後の対象・候補地点・探索状態・順序・現在地点の復元規則を正式文書で定める必要がある。
- 現在の Requirements / Specification は同一マップ内距離を最適化せず、Mob の候補集合から一地点だけを選ぶ契約である。B モブの全候補地点を巡る最短探索ルートとは競合するため、既存のマップ間巡回ルートとの関係、探索の開始地点・終了条件・距離尺度を上流工程から再定義する必要がある。
- 一般モブは候補から一地点を選び、B モブは全候補を探索するため、候補地点の route-selected / visited 状態とランク別ルート規則を正式文書で再定義する必要がある。
- [Requirements](docs/requirements/requirements.md) の文書ヘッダーは `Requirements Draft`、[Design](docs/design/design.md) のヘッダーは「次回 Design Review 待ち」のままだが、対応する最終レビューは `READY` と記録している。承認状態の文書メタデータを同期するかは未決定であり、現時点ではレビュー成果物を工程判定の根拠とする。
- 実装は存在するが、承認済み Specification / Design に対する独立した Implementation Review と、その検証証跡は未作成。適合性・残存差分の有無は未確定。

## Historical Decisions

- 初期の Treasure Compass を基盤に Mob Compass を追加したが、両者の cardinality と lifecycle は異なるため、共通の対象モデルへ統合せず別 product/session root とする判断が採用された。
- 共有周回は「リーダーがチャット案内を出力し、別のリーダーが取り込める」範囲に限定した。リアルタイム共有や権限移譲を導入しないことで、同期・認証基盤を v1 の責任境界から除外している。
